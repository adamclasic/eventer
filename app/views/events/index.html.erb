<h2>all Events</h2>
<% @all_events.each do |event|%>
    <p> Event id: <%= event.id%></p>
    <p> Event: <%= event.title%></p>
    <p> Description: <%= event.description%></p>
    <p> Date: <%= event.date%></p>
    <p> Created by: <%= event.creator.username%></p><br>
<% end %>